# Base de datos (en Docker Compose del repo se usa el servicio mongo; en local:)
MONGODB_URI=mongodb://localhost:27017/cagadas

# Firma de JWT (obligatoria en producción / Dokploy)
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
